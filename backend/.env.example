# Flask Server Configuration
FLASK_APP=app.py
FLASK_ENV=development
FLASK_DEBUG=True
PORT=5000

# Database Configuration (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=societyhub
DB_PORT=3306

# Secret key for JWT / session signature
JWT_SECRET=super_secret_jwt_signing_key_change_me_in_production
